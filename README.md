# facial-recognition-program
1. make the nutika.exe through auto-py-to-exe-launcher.bat.
2. make from open_cv.py to open_cv.exe at the conditions about standalone and hidden consol mode exe, the most below button through nutika.exe.
3. After finishing the operation to make the open_cv.exe, use the open_cv.exe when referring the manual.
